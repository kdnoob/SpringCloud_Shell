server:
  port: 8762
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
     defaultZone: http://eureka1:8761/eureka/
    register-with-eureka: false
#    fetch-registry: false
  instance:
    hostname: localhost