server:
  port: 8761
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
     defaultZone: http://eureka2:8762/eureka/
    register-with-eureka: false
#    fetch-registry: false
  instance:
    hostname: localhost